import styled from "styled-components";

import { Container } from "../common";

const MainForecastWrapper = styled(Container)`
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
`;

export default MainForecastWrapper;
