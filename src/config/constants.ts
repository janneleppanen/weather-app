export enum TemperatureScales {
  celcius = "Celcius",
  fahrenheit = "Fahrenheit"
}
export enum Languages {
  en = "en",
  fi = "fi"
}
export enum Themes {
  light = "Light",
  dark = "Dark"
}

export const AppName = "Weather";
